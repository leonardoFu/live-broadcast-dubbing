[pytest]
markers =
    unit: Unit tests - fast, isolated, mocked dependencies
    contract: Contract tests - API/event schema validation
    integration: Integration tests - cross-service workflows
    e2e: End-to-end tests - full pipeline validation

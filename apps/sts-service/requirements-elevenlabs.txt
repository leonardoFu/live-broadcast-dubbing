# Production dependencies for STS Service with ElevenLabs TTS
# Lightweight version - no Coqui TTS, no CUDA required
#
# Install with: pip install -r requirements-elevenlabs.txt

# Core Python packages
numpy==1.26.4
scipy==1.13.1
pyyaml==6.0.3
pydantic==2.12.5

# Audio processing
soundfile==0.12.1
pydub==0.25.1
librosa==0.10.2.post1

# PyTorch (for faster-whisper ASR)
# On ARM64 (Apple Silicon), regular torch is CPU-only
# On x86_64, this will download CUDA version but we won't use GPU
torch==2.2.2
torchaudio==2.2.2

# ASR (Automatic Speech Recognition)
faster-whisper==1.2.1
ctranslate2==4.6.2
tokenizers>=0.19,<0.20

# Translation
deepl==1.27.0

# TTS - ElevenLabs only (no Coqui TTS)
elevenlabs>=0.2.0,<2.0

# Socket.IO server
python-socketio==5.16.0
python-engineio==4.13.0
aiohttp==3.13.2

# Web framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
starlette==0.50.0

# Observability
prometheus-client==0.23.1
structlog==23.3.0
python-json-logger==4.0.0

# Utilities
rich==14.2.0
